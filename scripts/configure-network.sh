#!/usr/bin/env bash
set -euo pipefail

echo "Network configuration is intentionally manual in this first pass."
echo "Use NetworkManager profiles to keep the hidden TS1 AP separate from uplink Wi-Fi."

